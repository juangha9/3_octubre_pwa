project_id = "acvavpzdeichdvsgblcn"

# Única función OSINERGMIN: la usa el cron (x-cron-secret) Y el botón manual
# (superadmin). Valida el rol internamente y responde el preflight OPTIONS del
# navegador; por eso se desactiva verify_jwt del gateway (si no, el preflight
# CORS se rechaza).
[functions.osinergmin-cron]
verify_jwt = false

# Spike de validación (TEMPORAL): compara Facilito en vivo vs el snapshot del
# Excel. Lo dispara el superadmin desde el panel de OsinergminPage. Borrar junto
# con la función cuando termine la validación de fuente.
[functions.osinergmin-spike]
verify_jwt = false
